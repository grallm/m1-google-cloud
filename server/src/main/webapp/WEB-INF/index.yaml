
indexes:
  # Own a Post
  - kind: Post
    properties:
      - name: owner
      - name: date
        direction: desc

  # Followings
  - kind: Follow
    properties:
      - name: user
      - name: following
